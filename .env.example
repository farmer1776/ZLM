# Zimbra Lifecycle Manager — Environment Variables
# Copy to .env and customize for your deployment.
# NOTE: Sensitive values (passwords, keys) should use podman secrets instead.
# See DEPLOY.md for secret creation instructions.

# --- Database ---
ZLM_DB_HOST=db
ZLM_DB_PORT=3306
ZLM_DB_NAME=zimbra_lifecycle
ZLM_DB_USER=zlm
# ZLM_DB_PASSWORD=           # Use podman secret 'zlm_db_password' instead

# --- Application ---
ZLM_DEBUG=false
ZLM_ALLOWED_HOSTS=*
# ZLM_SECRET_KEY=            # Use podman secret 'zlm_secret_key' instead

# --- Zimbra ---
# ZLM_ZIMBRA_PASSWORD=       # Use podman secret 'zlm_zimbra_password' instead

# --- Gunicorn ---
GUNICORN_BIND=0.0.0.0:8000
GUNICORN_WORKERS=2
GUNICORN_LOG_LEVEL=info
